import { Component } from '@angular/core';
import { Quote } from './quote';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

//   quotes:string[];

//   constructor() {
//     this.quotes = ["If an pportunity doesn't knock, build a door", "Be yourself everybody else is taken", "So many books, so little time"]
// } 
}

